@"
# Server .env.example — DO NOT commit real secrets
# Copy this to .env locally for testing, and set real values in Render UI.

# Server
PORT=5587
NODE_ENV=development

# MongoDB
DB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net
DB_NAME=FingerTip

# Frontend origin (used by CORS)
FRONTEND_URL=https://fingertip.co.in

# Auth / security
JWT_SECRET=replace_with_a_long_random_secret

# Optional third-party services
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Email (optional)
EMAIL_HOST=smtp.example.com
EMAIL_USER=you@example.com
EMAIL_PASS=your-email-password

# Extra: comma-separated additional origins you may want to allow (optional)
ADDITIONAL_ORIGINS=
"@ | Out-File -FilePath .\.env.example -Encoding utf8
